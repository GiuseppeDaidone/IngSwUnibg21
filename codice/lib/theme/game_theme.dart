import 'package:flutter/material.dart';
// Classe che contiene il tema di tutti i componenti dell'applicazione (colore sfondo, colore pulsanti, stile pulsanti etc)

class GameTheme {
  static const Color primaryColor = Colors.green;
  static Color secondaryColor = Colors.green[700]!;
  static Color buttonColor = Colors.green[900]!;
}
